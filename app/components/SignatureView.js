// SignatureView.js

import { requireNativeComponent } from 'react-native';

// requireNativeComponent automatically resolves 'RNTMap' to 'SignatureViewManager'
module.exports = requireNativeComponent('SignatureView', null);
